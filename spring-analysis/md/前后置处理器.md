
BeanFactoryPostProcessor:处理修改bean定义信息的后置处理器，这个时候bean还没有初始化，只是定好了BeanDefinition，在BeanFactoryPostProcessor接口的postProcessBeanFactory方法中，我们可以修改bean的定义信息，例如修改属性的值，修改bean的scope为单例或者多例。
BeanPostProcessor:对初始化bean前后进行一些修改操作。
PostProcessorRegistrationDelegate.registerBeanPostProcessors(beanFactory, this)
